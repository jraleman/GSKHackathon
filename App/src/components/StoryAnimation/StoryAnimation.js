import React, { Component } from 'react';

export default class StoryAnimation extends Component {
  constructor (props) {
    super(props);
    this.state = {};
    return ;
  }

  render () {
    return (
      <div>
        { 'this is the StoryAnimation component' }
      </div>
    );
  }
}
