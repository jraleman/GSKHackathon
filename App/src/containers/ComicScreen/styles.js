const styles = theme => ({
  root: {
    flexGrow: 1,
    paddingTop: 12,
    paddingBottom: 12
  },
  paper: {
    padding: theme.spacing.unit * 2,
    textAlign: 'center',
    color: theme.palette.text.secondary,
  },
});

export default styles;
