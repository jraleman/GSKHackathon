const defaultState = {
  storyTitles: '',
  pages: [],
  currentPage: undefined
};

const story = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default story;
